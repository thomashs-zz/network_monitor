# encoding: UTF-8
pt-BR:
  errors:
    messages:
      invalid_date: "não é uma data válida"
      invalid_time: "não é um horário válido"
      invalid_datetime: "não é uma data/hora válida"
      is_at: "deve ser antes de %{restriction}"
      before: "deve ser antes %{restriction}"
      on_or_before: "deve ser em %{restriction} ou antes"
      after: "deve ser depois de %{restriction}"
      on_or_after: "deve ser em %{restriction} ou depois"
  validates_timeliness:
    error_value_formats:
      date: '%d/%m/%Y'
      time: '%H:%M:%S'
      datetime: '%d/%m/%Y %H:%M:%S'